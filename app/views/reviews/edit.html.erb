<%= form_for @review, url: book_review_path do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
    <%= f.text_field :content %>
  <%= f.submit "Save", class: "btn btn-primary" %>
<% end %>